{
  "_meta": {
    "per_experiment_train_time_sec": 1800,
    "note": "Baseline usada no pré-filtro SLA para train_time_sec. Ajuste conforme hardware e dataset reais.",
    "time_estimation": {
      "baseline_train_time_sec": 1800,
      "reference_batch_size": 16,
      "default_precision": "fp16",
      "optimizer_factors": {
        "adam": 1.0,
        "adamw": 1.0,
        "sgd": 0.9,
        "bert_adam": 1.08
      },
      "precision_factors": {
        "fp32": 1.15,
        "fp16": 1.0,
        "bf16": 0.98
      }
    }
  },
  "description": "Configuração completa de Grid Search (432 combinações)",
  "experiment_base": "config/experiments/BertPLI.config",
  "output_dir": "output/experiments/grid_search",
  "parallel_workers": 2,
  
  "hyperparameters": {
    "learning_rate": [1e-5, 2e-5, 3e-5, 5e-5],
    "batch_size": [8, 16, 32],
    "optimizer": ["adam", "adamw", "sgd", "bert_adam"],
    "dropout": [0.1, 0.2, 0.3],
    "seed": [42, 123, 456]
  },
  
  "notes": [
    "Grid completo: 4 x 3 x 4 x 3 x 3 = 432 experimentos",
    "Tempo estimado: ~72-108 horas com parallel=2",
    "Memória recomendada: 32GB RAM mínimo",
    "Use --parallel 2 para evitar OOM em sistemas com 32GB"
  ],
  
  "recommendations": {
    "parallel_workers": 2,
    "max_parallel_workers": 4,
    "min_ram_gb": 32,
    "safe_ram_gb": 64
  }
}
