{
  "_meta": {
    "per_experiment_train_time_sec": 1800,
    "note": "Baseline usada no pré-filtro SLA para train_time_sec. Ajuste conforme hardware e dataset reais.",
    "time_estimation": {
      "baseline_train_time_sec": 1800,
      "reference_batch_size": 16,
      "default_precision": "fp16",
      "optimizer_factors": {
        "adam": 1.0,
        "adamw": 1.0,
        "sgd": 0.9,
        "bert_adam": 1.08
      },
      "precision_factors": {
        "fp32": 1.15,
        "fp16": 1.0,
        "bf16": 0.98
      }
    }
  },
  "description": "Grid Search Mínimo - Teste de Pipeline com 6 combinações (3 hiperparâmetros × 2 ambientes)",
  "experiment_base": "config/experiments/BertPLI.config",
  "output_dir": "output/experiments/grid_search",
  "parallel_workers": 1,
  
  "hyperparameters": {
    "learning_rate": [1e-5],
    "batch_size": [8],
    "optimizer": ["adam", "adamw", "bert_adam"]
  },

  "environments": {
    "_comment": "BL-W2: capacidade por ambiente (parallel_workers) consumida por gridsearch/executor.py::_gpu_for",
    "active": ["local", "colab"],
    "details": {
      "local": {
        "label": "Local RTX 3090",
        "gpu": "NVIDIA RTX 3090",
        "vram_gb": 24,
        "cost_per_hour_usd": 0.04,
        "co2_factor_kg_per_kwh": 0.233,
        "parallel_workers": 2
      },
      "colab": {
        "label": "Google Colab T4",
        "gpu": "NVIDIA T4",
        "vram_gb": 16,
        "cost_per_hour_usd": 0.00,
        "co2_factor_kg_per_kwh": 0.082,
        "parallel_workers": 1
      }
    }
  },
  
  "notes": [
    "Configuração MÍNIMA para teste de pipeline: 1 × 1 × 3 (otimizadores) × 2 (ambientes) = 6 experimentos",
    "Inclui a dimensão 'environment' (BL-W2) para validar a alocação de GPU consciente da capacidade por ambiente",
    "Tempo estimado: ~30 minutos por experimento (depende do dataset)",
    "Memória estimada: ~4.5 GB",
    "Objetivo: Verificar que todo o fluxo funciona antes de rodar grids maiores"
  ]
}
